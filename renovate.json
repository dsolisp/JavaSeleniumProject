{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "labels": ["dependencies"],
  "schedule": ["every weekend"],
  "prConcurrentLimit": 5,
  "semanticCommits": "enabled",
  "commitMessagePrefix": "chore(deps):",

  "packageRules": [
    {
      "description": "Maven dependencies — group minor+patch for auto-merge",
      "matchManagers": ["maven"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "Maven dependencies (minor+patch)",
      "automerge": true,
      "automergeType": "pr",
      "platformAutomerge": true
    },
    {
      "description": "Selenium — keep selenium-java + selenium-support in sync",
      "matchPackagePatterns": ["^org.seleniumhq.selenium"],
      "groupName": "Selenium",
      "automerge": false
    },
    {
      "description": "JUnit 5 — keep all junit-jupiter in sync",
      "matchPackagePatterns": ["^org.junit.jupiter"],
      "groupName": "JUnit 5",
      "automerge": false
    },
    {
      "description": "Maven major updates — require manual review",
      "matchManagers": ["maven"],
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "labels": ["dependencies", "major-update"]
    },
    {
      "description": "GitHub Actions — auto-merge patch",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true,
      "groupName": "GitHub Actions"
    },
    {
      "description": "Security updates — immediate, high priority",
      "matchCategories": ["security"],
      "schedule": ["at any time"],
      "automerge": false,
      "labels": ["dependencies", "security"],
      "prPriority": 10
    }
  ],

  "vulnerabilityAlerts": { "enabled": true, "labels": ["security"] },
  "osvVulnerabilityAlerts": true
}
