## Zero-to-Hero Tutorial (canonical path)

The full tutorial lives here:

- `docs/ZERO_TO_HERO_TUTORIAL.md`

This file exists to provide a **stable canonical path** across the portfolio:

- `docs/ZERO_TO_HERO.md` (this file) → canonical pointer
- `docs/ZERO_TO_HERO_TUTORIAL.md` → full tutorial content
- `docs/ZERO-TO-HERO.md` → legacy stub (redirects to this pointer)

If you’re starting from zero, open `docs/ZERO_TO_HERO_TUTORIAL.md`.

**Implementation-first reading:** `pom.xml`, `src/main/java/com/automation/config/Settings.java`, `utils/WebDriverFactory.java`, and `src/test/java/com/automation/`.
